# S6: Selective MIMO State Space Model

## Overview

S6 combines the best ideas from S4D, S5, and Mamba into a single SSM layer designed to slot into the ripple mixer architecture (where local context is already handled by multi-scale convolution).

**Base implementation:** S4D (`s4/models/s4/s4d.py`) — we copy and make targeted edits, not a rewrite.

## What We Take From Each

| Source | What | Why |
|--------|------|-----|
| **S4D** | Base code, HiPPO-LegS A initialization, complex diagonal state | Proven init, clean impl |
| **S5** | Dense B (P,H) and C (H,P) — MIMO cross-channel mixing | Cross-feature state mixing without full matrix A |
| **Mamba** | Input-dependent dt projected from input | Selectivity — content-based gating of state updates |
| **Ours** | SiLU activation (no GELU), no GLU post-processing, no local conv | Ripple's msconv handles local; SiLU matches our attention stage |

## Feature Bank on B (from LUNA)

SSMs tend to drown in their own noise over long sequences — the state accumulates everything indiscriminately. A LUNA-style feature bank on the B projection gives the SSM learned nonlinear selectivity over what to integrate into state.

Instead of a linear B projection:
```
# Before (dense linear):
Bu = B @ u                    # projects everything into state

# After (feature bank):
Bu = phi_B(u)                 # learned nonlinear projection selects what enters state
```

`LearnableFeatureMap` (from `zoology/zoology/mixers/luna.py`):
- M projection directions with learned weights (default M=4)
- Shared channel MLP (SiLU activated, nonneg=False): scalar → L channels, where L = P // M
- Output dim D = M * L = P (state dimension)
- ch_rms normalization enabled

**B feature bank:** `phi_B: H → P` — learns what input features to integrate into state
**C readout:** static complex MIMO `C: (H, P)` + input-dependent gate — trusts that B selected the right things to write

The feature bank adds modest parameter cost (~1.7k params for H=128, P=64) but gives content-dependent nonlinear filtering that a plain linear cannot provide.

## What We Remove From S4D

1. **FFT convolution** — replaced by parallel scan (Triton kernel). Input-dependent dt breaks LTI, so no convolution mode.
2. **GELU + Conv1d(H→2H) + GLU output** — replaced by SiLU. The GLU channel mixing is unnecessary because MIMO B/C already mix channels.
3. **Per-channel SISO structure** — replaced by shared MIMO state.
4. **`DropoutNd`** — use standard dropout or none.

## Architecture

### Parameters

```
A:          (P,) complex diagonal     — HiPPO-LegS eigenvalues (from S4D/S5 init)
phi_B:      LearnableFeatureMap(H→P)  — feature bank input projection (LUNA-style, M=4, L=P//M, act=silu, nonneg=False)
C:          (H, P) complex           — static MIMO readout matrix (from S5)
c_proj:     nn.Linear(H, P)          — input-dependent C gating projection
D:          (H,) real                 — skip connection
x_proj:     nn.Linear(H, P+P+P//2)  — fused projection for [dt, lam, theta] (one cuBLAS call)
log_dt_bias: (P,) real               — bias for dt, init in [log(dt_min), log(dt_max)]
b_norm:     RMSNorm(P)               — learnable RMSNorm on B projection (QK-norm style)
c_norm:     RMSNorm(P)               — learnable RMSNorm on C gate (QK-norm style)
b_bias:     (P,) real, init=1.0      — additive bias on B after norm (Mamba-3: critical w/ trapezoidal)
c_bias:     (P,) real, init=1.0      — additive bias on C gate after norm (Mamba-3: critical w/ trapezoidal)
```

Where:
- H = d_model (input/output dimension)
- P = d_state (state dimension, e.g. 64)

### Discretization: Learned Trapezoidal

Based on our existing TrapSSM implementation (`src/heuristic_secrets/models/backbone.py:1495`). Instead of fixed bilinear transform, we use a **learned, input-dependent interpolation** between forward and backward Euler:

```
dt     = softplus(silu(dt_proj(u)) + log_dt_bias)   # (B, L, P) input-dependent
lam    = sigmoid(lambda_proj(u))                      # (B, L, P) interpolation weight
alpha  = exp(dt * A)                                  # (B, L, P) decay factor
beta   = (1 - lam) * dt * alpha                       # weight on PREVIOUS injection (lookback)
gamma  = lam * dt                                      # weight on CURRENT injection
```

State update uses both current and previous B*u:
```
Bu     = b_norm(phi_B(u)) + b_bias                     # feature bank + norm + bias
Bu     = apply_rotary(Bu, cum_theta)                   # RoPE before shift
Bu_prev = pad(Bu[:-1], left=1)                        # shifted AFTER rotation
inject = gamma * Bu + beta * Bu_prev                  # trapezoidal blend
h[t]   = alpha[t] * h[t-1] + inject[t]               # state update via parallel scan
```

Lambda init: `sigmoid(2.0) ≈ 0.88`, biased toward forward Euler (current input). The model learns to look back more where needed.

### Data-Dependent RoPE on B and C gate

Following our TrapSSM pattern (`backbone.py:1485-1491`):
```
theta       = x_proj(u)[theta_slice]                  # (B, L, P//2) per position
dt_half     = dt.view(B, L, P//2, 2).mean(-1)         # average paired dt dims → (B, L, P//2)
delta_theta = dt_half * theta                          # scale by averaged timestep
cum_theta   = cumsum(delta_theta, dim=1)               # cumulative rotation
Bu          = apply_rotary(Bu, cum_theta)              # rotate B in state space
Bu_prev     = pad(Bu[:, :-1], left=1)                  # shift AFTER rotation (pos t-1 keeps its angle)
c_gate      = apply_rotary(c_gate, cum_theta)          # rotate C gate to match state
```

This gives the SSM data-dependent frequency modulation — positions with larger dt rotate faster through state space. dt is (B,L,P) but theta is (B,L,P//2), so we average adjacent pairs of dt to get the P//2 rotation scales.

### Forward Pass: Parallel Scan

```
Input: u (B, L, H)

1. Feature bank + fused projections:
   Bu_raw = phi_B(u)                                   # (B, L, P) — nonlinear feature bank
   dt_raw, lam_raw, theta = x_proj(u).split(...)       # fused Linear(H, P+P+P//2)

2. Activations:
   dt    = softplus(silu(dt_raw) + log_dt_bias)        # (B, L, P)
   lam   = sigmoid(lam_raw)                            # (B, L, P)
   Bu    = b_norm(Bu_raw) + b_bias                     # (B, L, P) — norm + bias on feature bank output

3. Data-dependent RoPE + shift:
   dt_half   = dt.view(B,L,P//2,2).mean(-1)            # (B, L, P//2)
   cum_theta = cumsum(dt_half * theta, dim=1)           # (B, L, P//2)
   Bu        = apply_rotary(Bu, cum_theta)              # rotate first
   Bu_prev   = pad(Bu[:, :-1], left=1)                  # then shift (pos t-1 keeps its rotation)

4. Trapezoidal discretization:
   alpha  = exp(dt * A)                                # (B, L, P) complex decay
   inject = lam*dt * Bu + (1-lam)*dt*alpha * Bu_prev   # trapezoidal blend

5. Parallel scan (Triton kernel):
   h[t] = alpha[t] * h[t-1] + inject[t]               # elementwise (diagonal A)
   Returns all h[t]: (B, L, P) complex

6. Readout (static MIMO C + input-dependent gate):
   c_gate = c_norm(silu(c_proj(u))) + c_bias           # (B, L, P) — input-dependent gate
   c_gate = apply_rotary(c_gate, cum_theta)            # rotate gate to match state
   h_gated = h * c_gate                                # (B, L, P) complex * real
   y = einsum('hp,blp->blh', C, h_gated).real          # (B, L, H) — MIMO readout
   y = y + D * u                                       # skip connection

7. Activation:
   y = silu(y)
### Projection Activations & Norms

**B side:** Feature bank output goes through `b_norm(phi_B(u)) + b_bias`. The feature bank's ScalarMLP already applies SiLU internally. RMSNorm prevents magnitude drift in state space.

**C side:** `c_norm(silu(c_proj(u))) + c_bias` — linear projection, SiLU activation, RMSNorm, additive bias. This gates the static MIMO readout.

**dt:** `softplus(silu(dt_raw) + log_dt_bias)` — from fused x_proj.

**BC biases** (init=1.0, learnable, added after norm): From Mamba-3 — these synergize with trapezoidal discretization and **eliminate the need for a short conv** before the SSM. Without biases, trapezoidal discretization underperforms Euler. With biases, it strictly dominates.

### Triton Kernel: Parallel Associative Scan

The scan is the bottleneck. Python sequential is O(L) with no parallelism. We need a Triton kernel implementing the associative scan with the binary operator:

```
(a_i, b_i) ⊕ (a_j, b_j) = (a_j * a_i, a_j * b_i + b_j)
```

Where a = A_bar (complex diagonal) and b = Bu (complex). All operations are elementwise since A is diagonal.

**Kernel structure:**
- One program per (batch, state_dim) pair
- Each program scans over L positions
- Up-sweep (reduce) + down-sweep (propagate) pattern
- Complex arithmetic throughout (store as float2 or pair of floats)
- Support variable A_bar per position (input-dependent dt means A_bar varies)

## File Plan

### Copy from S4D
```
s4/models/s4/s4d.py → src/s6/s6.py
```

### New files
```
src/s6/__init__.py          — exports
src/s6/scan.py              — Triton parallel scan kernel
src/s6/init.py              — HiPPO init (copy from s5-pytorch/s5/init.py, strip JAX compat)
```

### Specific Edits to s4d.py → s6.py

#### S4DKernel → S6Kernel

1. **Remove** `forward(L)` Vandermonde/FFT kernel generation entirely
2. **Replace** dense B with `LearnableFeatureMap` feature bank (phi_B: H→P, M=4, act=silu, nonneg=False). C is static complex MIMO with input-dependent gate.
3. **Change** A init from S4D-Lin to HiPPO-LegS eigenvalues (via `make_DPLR_HiPPO`)
4. **Add** fused `x_proj = nn.Linear(H, P+P+P//2)` for dt, lam, theta (one cuBLAS call)
5. **Add** `log_dt_bias` parameter `(P,)`, init uniform in [log(dt_min), log(dt_max)]
6. Lambda bias init: `x_proj.bias[P:2P] = 2.0` (sigmoid(2)≈0.88)
7. Theta region: zero weights and bias init (starts from zero rotation)
8. **Add** `b_norm = RMSNorm(P)`, `c_norm = RMSNorm(P)` — learnable norms on B/C projections
8b. **Add** `b_bias = nn.Parameter(torch.ones(P))`, `c_bias = nn.Parameter(torch.ones(P))` — additive biases after norm (Mamba-3)
9. **Add** `forward(u)` method: project → feature bank → RoPE → trapezoidal discretize → scan → readout

#### S4D → S6

1. **Remove** FFT convolution in forward
2. **Remove** `output_linear` (Conv1d + GLU)
3. **Change** activation from GELU to SiLU
4. **Remove** DropoutNd
5. **Forward** becomes: `kernel.forward(u)` → skip connection → SiLU → optional out_proj

### Zoology Integration

```
zoology/zoology/mixers/s6.py  — thin wrapper matching zoology interface
                                __init__(d_model, layer_idx, **kwargs)
                                forward(x: (B,L,D)) -> (B,L,D)
```

Ripple integration as new stage `'s6'` in ripple_attention.py, config `ripple-s6a` = `"s6,attn"`.

## Open Questions

1. **Trapezoidal paper** — user will provide, may refine discretization details
2. ~~**Feature bank sizing**~~ — RESOLVED: M=4 (default), L=P//M. Search for ideal M per task.
3. **State dimension P** — default 64 (matching S4D), or scale with d_model?
4. **dt projection** — full rank via fused x_proj. Low-rank bottleneck if needed later.
5. **Learnable A** — S4D learns A, S5 learns A. Keep learnable? (Currently yes)
6. **Output projection** — do we need a final Linear(H, H) or is SiLU enough?
7. ~~**Feature bank activation**~~ — RESOLVED: SiLU, nonneg=False. Not doing PD kernel attention, need signed values.
8. ~~**ch_rms on feature banks**~~ — RESOLVED: Yes, enabled by default.
